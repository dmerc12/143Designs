import PropTypes from 'prop-types';

export const ManageRequests = ({ toast }) => {
    return (
        <>
        
        </>
    )
};

ManageRequests.propTypes = {
    toast: PropTypes.object.isRequired
};
