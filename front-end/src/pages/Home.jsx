import PropTypes from 'prop-types';

export const Home = ({ toast }) => {
    document.title = "Home";

    return(
        <>
        
        </>
    )
};

Home.propTypes = {
 toast: PropTypes.object.isRequired
};
