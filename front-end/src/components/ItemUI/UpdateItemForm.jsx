import PropTypes from 'prop-types';

export const UpdateItemForm = ({ toast }) => {
    return (
        <>
        
        </>
    )
};

UpdateItemForm.propTypes = {
    toast: PropTypes.object.isRequired
};
