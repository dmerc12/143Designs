import PropTypes from 'prop-types';

export const ManagePastWork = ({ toast }) => {
    return (
        <>
        
        </>
    )
};

ManagePastWork.propTypes = {
    toast: PropTypes.object.isRequired
};
