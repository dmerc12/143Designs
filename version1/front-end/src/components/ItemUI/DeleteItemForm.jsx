import PropTypes from 'prop-types';

export const DeleteItemForm = ({ toast }) => {
    return (
        <>
        
        </>
    )
};

DeleteItemForm.propTypes = {
    toast: PropTypes.object.isRequired
};
